export type SearchInput = {
  search: string;
};

export type CategoriesDropdownType = {
  categories: string;
};

export type CarouselPropsType = {
  src: string;
  altText: string;
  caption: string;
};
